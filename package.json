{
  "name": "scrap-shift",
  "version": "1.1.11",
  "description": "A neon pixel-art arena survival shooter for RUN.world.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "allowScripts": {
    "@firebase/util": false,
    "@series-inc/rundot-game-sdk@5.24.0": true,
    "esbuild@0.25.12": true,
    "fsevents": false,
    "protobufjs": false
  },
  "scripts": {
    "dev": "vite",
    "dev:playground": "cross-env RUNDOT_PLAYGROUND=1 vite",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint src scripts vite.config.ts --diagnostic-level=error --max-diagnostics=none",
    "typecheck": "tsc --noEmit",
    "thumbnail": "node scripts/generate-thumbnail.mjs",
    "simulate": "node --experimental-strip-types scripts/simulate.ts",
    "test": "node scripts/check-version.mjs && node --experimental-strip-types scripts/check-safe-area.ts && node --experimental-strip-types scripts/check-save-schema.ts && node --experimental-strip-types scripts/check-daily-rewards.ts && npm run simulate",
    "build": "npm run typecheck && vite build && node scripts/check-build.mjs",
    "check": "npm run format:check && npm run lint && npm run test && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@series-inc/rundot-game-sdk": "5.24.0",
    "firebase": "12.16.0",
    "pixi.js": "^8.18.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "cross-env": "^7.0.3",
    "typescript": "^5.8.0",
    "vite": "^6.3.5"
  }
}
